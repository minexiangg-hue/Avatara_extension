# Avatara 0.3 · 验证记录

基线记录日期：2026-09-10。新增 Agent 变更日期：2026-09-22。范围：新项目 `Avatara_next`。原项目未修改。

## 2026-09-22 原生工具 Agent：本轮证据

真实模型冒烟与 localhost 确认卡片交互已完成下面列出的用例。最终全量 `node --test tests/*.test.mjs`：**90 / 90 通过**；`node scripts/check.mjs`：27 个 JavaScript 模块、Manifest 与 HTML 资源引用通过。下方 2026-09-10 的 52 项通过仅是历史基线。

新增覆盖原生多轮工具协议、普通聊天不读取个人集合、来源后续追问、参数／未知工具、6 轮与 10 次上限、超时、隐私与引用、确认幂等与事务、卡片转义、跨标签演示锁等。真实云端验收还发现并修复了中文标点被 URL 规范化吞入的问题。凭据回归覆盖来源 URL 与普通聊天内的完整会话密钥脱敏，以及更换 API 来源／连接测试时必须明确提供新服务密钥、保存失败恢复原绑定。

`node scripts/package.mjs` 已生成 `dist/avatara-0.3.0.zip`，含 19 个生产文件。保留 0.2.0 旧包用于回退；没有删除旧项目或已有数据。

### 真实 DeepSeek + 合成 Chrome 运行环境

执行 `node scripts/live-agent-smoke.mjs <凭据路径>`，真实调用 `deepseek-v4-flash` 的原生 `tool_calls`。Chrome API、记忆、目标与当前页均为受控合成夹具；没有读取真实浏览器历史或真实网页正文。

| 用例 | 实际观察 |
| --- | --- |
| 连接检查 | 模型返回有效响应。 |
| 一般知识对话 | 1 次模型请求，0 个工具、0 个记忆来源；没有强制查历史。 |
| 找到蓝湾实验记录并读正文结尾 | 3 次模型请求，依次调用 `search_memories`、`read_memory`，正确回答合成暗号并附唯一 `[1]` 来源。 |
| 创建目标提案 | 2 次模型请求生成 `create_goal` 待确认卡片，确认前目标数量不变。 |
| 批准创建与重复批准 | 第一次批准后目标数量从 1 增为 2；重复批准仍为 2，没有重复创建。 |
| 推进已有目标提案 | 3 次模型请求，先 `list_goals` 获取真实夹具 ID，再提出 `advance_goal`；确认前进度仍为 0。 |
| 批准进度与重复批准 | 进度从 0 增为 1；重复批准仍为 1。 |
| 读取合成当前页 | 2 次模型请求，调用 `read_current_page`，正确回答合成色彩「雾蓝」并附来源；记忆数量未增加。 |
| 进度事件与凭据隔离 | 收到带请求 ID 的运行时进度事件；本用例持久状态中未出现会话密钥。 |

该层证明真实模型能够选择和组合工具，并与新运行时的确认事务配合。`read_current_page` 的结果来自模拟 Chrome API，所以它不证明已安装扩展真的获得了 `activeTab`、执行了页面脚本或读取了真实当前页。6 轮／10 次上限、错误参数、权限失败等异常分支仍以本轮完整自动化回归结果为准。

### 真实浏览器中的合成确认卡片

入口：`http://127.0.0.1:4321/src/preview/agent-fixture.html`。用 `node scripts/preview.mjs --port 4321` 启动专用端口，手动点击明确的载入／重置按钮后进入对话。卡片和执行摘要由夹具构造，页面显式说明没有调用模型、扩展或真实浏览器工具。

| 用户操作 | 实际观察 |
| --- | --- |
| 点击创建目标卡片的确认按钮 | 演示目标数量从 1 增为 2，卡片进入已确认状态。 |
| 点击进度卡片的拒绝按钮 | 卡片显示已取消，原目标仍保持 1 / 4。 |
| 刷新页面 | 确认／取消状态与对应目标结果保留。 |
| 输入普通本地对话 | 不再把浏览记录强行塞进一般聊天回答。 |
| 390 × 844 侧栏宽度 | 已修正提示区挤压输入框的问题；输入框底部约 749 px，底部导航从约 780 px 开始，无文档横向溢出。 |
| 控制台与环境恢复 | 未捕获 error / warn，测试结束恢复原视口并关闭临时标签页。 |

该层证明共享 UI 的确认／拒绝事件、状态呈现和持久化闭环。它不证明模型生成了这些卡片。执行摘要是工具执行的短状态，不是模型私有推理过程。

### 尚待完成的本轮验证

真实安装扩展的权限弹窗、当前页脚本注入、标签页可见范围、后台挂起恢复与云端 Agent 组合流程尚未证明通过；既有扩展页面接管限制仍是原生验收边界。不能把以上两个证据层合并表述为已完成原生 Chrome 端到端测试。

## 2026-09-10 历史基线

### 自动化功能测试

`node --test tests/*.test.mjs`：**52 项全部通过**。

- 核心：数据规范化、URL 清理、凭据遮蔽、历史合并、收藏保留、中英文检索、主题归类、目标状态。
- AI：上下文数量和长度限制、相关目标选择、引用范围及编号、无效响应、超时中止、错误脱敏。
- Chrome 运行层模拟：请求来源、可选权限、队列并发、会话密钥、历史导入、当前页采集、权限撤销、保留期、容量失败恢复与导出。
- 演示：合成数据隔离、持久化、目标流程、导入去重、离线对话、拒绝密钥、请求失败恢复。

`node scripts/check.mjs`：18 个 JavaScript 模块语法、Manifest 资源、HTML 引用通过。

### 真实 Chrome 中的共享界面

地址：`http://127.0.0.1:4318`。通过受支持的浏览器交互工具执行，使用合成数据，没有连接已安装扩展。

| 用例 | 实际观察 |
| --- | --- |
| 首页 → 记忆 → 目标 → 对话 → 设置 | 五个页面可进入，标题与内容对应。 |
| 中文检索「检索」 | 返回 3 条相关记录，排序与来源可见。 |
| 不存在的混合关键词 | 返回 0 条，并显示空结果提示。 |
| 收藏页面后刷新 | 收藏数量从 6 到 7，刷新后仍为 7。 |
| 创建「认真读完一本书」，目标 2 章 | 创建成功，初衷与单位保存。 |
| 两次记录进展 | 0% → 50% → 100%，完成后按钮禁用。 |
| 提问「帮我找关于 AI 助理的内容」 | 本机返回 5 条来源，数字与链接对应。 |
| 提问目标下一步 | 使用实际目标名称、初衷与进度，不推断完成情况。 |
| 两个演示标签之间保存页面 | 另一个已打开的首页自动从 12 更新为 13 条，无需刷新。 |
| 390 × 844 窄屏 | 首页目视检查；记忆、目标、对话、设置均无文档或主内容横向溢出。 |
| 错误日志 | 已验收页面未捕获 error / warn 日志。 |

视觉验收后加强了正文与辅助信息可读性；中文搜索保留输入法组合过程；聊天失败保留草稿；连接测试允许离开设置页；大量记忆采用分批展示。

### 真实 DeepSeek 请求

执行 `node scripts/live-ai-smoke.mjs <凭据路径>`，调用新实现的实际 Provider。密钥只在进程内使用。三次请求均使用合成资料：

1. 连接检查成功，模型 `deepseek-v4-flash`。
2. 从唯一合成记忆回答暗号「银杏书签」，返回正确 `[1]` 与唯一来源。
3. 根据合成目标「精读两篇测试文章」的 `0/2` 进度，给出合理下一步。

没有读取或发送真实浏览历史、目标、账户资料或页面内容。

### HTTP 与分发

预览服务器验证过资源白名单、路径穿越拒绝、脚本路径隔离、HEAD、POST 拒绝、Host 检查和 CSP（12 项 HTTP 检查通过）。

`node scripts/package.mjs` 生成纯扩展 ZIP，已用系统 ZIP 读取器检查目录：16 个生产文件，无凭据、演示数据或开发脚本。工作台、侧边栏、弹窗与所有图标引用存在。

### 尚未声称通过的项目

已安装扩展的原生 Chrome 端到端流程尚未完成：当前平台拒绝接管 `chrome-extension://` 页面。没有绕过该限制。localhost 界面测试、运行时模拟和独立云端请求均不等于原生流程通过。

加载新目录后仍需确认：首次权限弹窗、工具栏保存真实网页、侧边栏打开、真实历史导入、Service Worker 挂起恢复、Chrome 重启后的密钥清除，以及设置页的实际云端授权。
